import { VariableFalsepositon } from './variable-falsepositon';

describe('VariableFalsepositon', () => {
  it('should create an instance', () => {
    expect(new VariableFalsepositon()).toBeTruthy();
  });
});
